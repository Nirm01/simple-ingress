apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: podinfo
  namespace: flux-system
spec:
  interval: 1m
  path: ./flux-ingress
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: test-flux
